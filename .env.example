# Copy to .env for local `vercel dev`. Never commit the real .env — it's gitignored.

# Powers the AI coach relay at api/coach.ts (weekly reflection, readiness
# commentary, ask-the-coach). The app is fully functional without this —
# every programming decision (load, exercises, ladders) is deterministic
# and unaffected by whether this key is set.
# Get one free at https://aistudio.google.com/apikey
GEMINI_API_KEY=
